# Architecture docs

Deep per-subsystem docs — how a subsystem works *now*, not a changelog. One file per subsystem,
kept in sync with the code (git is the history).

Empty for now. Add a file when a subsystem is complex enough that the map in `CLAUDE.md` plus the
generated `Docs/api/` reference aren't enough — e.g. the extension loader, the multi-instance model,
the settings framework, the AI chat pipeline.
