<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FoxyBrowser716_WinUI.Controls.SettingsPage.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:generic="using:FoxyBrowser716_WinUI.Controls.Generic"
    mc:Ignorable="d">

    <Grid Background="Gray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" x:Name="BorderSearch" Background="Red">
            <StackPanel>
                <generic:FTextInput x:Name="InputSearch"/>
                <TextBlock x:Name="ResultBlock" TextWrapping="Wrap"/>
                <!-- settings name (top 5 or so), then "+ {number of results} more" or "no results found" -->
            </StackPanel>
        </Border>
        <Border Grid.Column="0" Grid.Row="2" x:Name="BorderCategories" Background="Blue">
            <ScrollViewer/>
        </Border>
    </Grid>
</UserControl>


