<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FoxyBrowser716.Controls.SettingsPage.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:generic="using:FoxyBrowser716.Controls.Generic"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid">
        <Grid.Background>
            <RadialGradientBrush x:Name="BackgroundGradient"
                MappingMode="RelativeToBoundingBox"
                Center="0,1" GradientOrigin="0,1">
                <GradientStop Color="Red" x:Name="BackgroundSecondary" Offset="0.0"/>
                <GradientStop Color="Gray" x:Name="BackgroundPrimary" Offset="0.75"/>
            </RadialGradientBrush>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" x:Name="BorderSearch" BorderThickness="2" CornerRadius="8" Margin="5">
            <StackPanel>
                <generic:FTextInput x:Name="InputSearch"/>
                <TextBlock x:Name="ResultBlock" TextWrapping="Wrap"/>
                <!-- settings name (top 5 or so), then "+ {number of results} more" or "no results found" -->
            </StackPanel>
        </Border>
        <Border Grid.Column="0" Grid.Row="2" x:Name="BorderCategories" VerticalAlignment="Top" BorderThickness="2" CornerRadius="8" Margin="5">
            <ScrollViewer VerticalAlignment="Top">
                <StackPanel x:Name="CategoryViewer" VerticalAlignment="Top"/>
            </ScrollViewer>
        </Border>
        <ScrollViewer Grid.Column="1" Grid.Row="2">
            <StackPanel x:Name="SettingsViewer"/>
        </ScrollViewer>
    </Grid>
</UserControl>


